import { createTheme } from "@mui/material";

export default createTheme({
  palette: {
    background: {
      default: "#F1F3F4",
    },
    primary: {
      main: "#14194c",
    },
  },
  typography: {
    fontFamily: "Mulish, sans-serif",
    h4: {
      fontWeight: 800,
    },
  },
});
